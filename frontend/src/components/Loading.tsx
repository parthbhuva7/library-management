'use client';

export default function Loading() {
  return (
    <p
      style={{
        color: 'var(--muted)',
        fontSize: 'var(--font-size-base)',
      }}
    >
      Loading...
    </p>
  );
}
